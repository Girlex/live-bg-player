# 🧪 新功能测试指南

## 📱 安装和运行

### 1. 安装 APK

APK 位置：`app/build/outputs/apk/debug/app-debug.apk`

```bash
# 通过 ADB 安装
adb install app/build/outputs/apk/debug/app-debug.apk

# 或者直接在手机上安装
```

---

## ✅ 测试清单

### 测试 1: ConfigActivity 新界面

**步骤**：
1. 打开应用
2. 查看配置页面

**预期结果**：
- ✅ 看到 "🎨 画面控件设置" 卡片
- ✅ 有三个复选框：
  - 显示实时时钟
  - 显示直播计时器
  - 显示弹幕文字
- ✅ 看到 "💡 V字手势切换视频" 提示卡片
- ✅ 提示卡片是橙色背景，包含使用说明

**截图位置**：ConfigActivity 底部

---

### 测试 2: 启用画面控件

**步骤**：
1. 选择一个视频文件
2. 勾选 "显示实时时钟"
3. 勾选 "显示直播计时器"
4. 勾选 "显示弹幕文字"
5. 点击 "🚀 确认并开始播放"

**预期结果**：
- ✅ 进入播放页面
- ✅ 视频正常播放
- ✅ 右下角显示实时时钟（格式：HH:mm:ss）
- ✅ 左下角显示直播计时器（从 00:00:00 开始递增）
- ✅ 顶部有弹幕文字滚动（"欢迎来到直播间"）

**验证点**：
- [ ] 时钟每秒更新
- [ ] 计时器每秒递增
- [ ] 弹幕从右向左滚动
- [ ] 控件不影响视频播放
- [ ] 控件透明度合适（不遮挡视频内容）

---

### 测试 3: V 字手势切换视频

**前置条件**：需要准备至少 2 个视频文件

**步骤**：
1. 在 ConfigActivity 选择第一个视频
2. 进入播放页面
3. 用手指在屏幕上画 V 字形：
   - 从屏幕中间偏上位置开始
   - 向**左下方**或**右下方**移动（至少 200px）
   - 再向**右上方**或**左上方**移动（至少 200px）
   - 整个动作在 **1 秒内**完成

**预期结果**：
- ✅ 识别成功后立即切换到第二个视频
- ✅ 切换过程无缝（无黑屏、无卡顿）
- ✅ 日志输出："检测到 V 字手势，切换视频"
- ✅ 日志输出："切换到下一个视频: 2"

**失败情况**（不应该触发切换）：
- ❌ 移动距离太短（< 200px）
- ❌ 动作太慢（> 1秒）
- ❌ 双指触摸
- ❌ 只画了半段（只有向下或只有向上）

**测试变体**：
- [ ] V 字向左：先右下再左上
- [ ] V 字向右：先左下再右上
- [ ] 快速画 V（应该成功）
- [ ] 慢速画 V（应该失败）
- [ ] 短距离画 V（应该失败）

---

### 测试 4: 禁用画面控件

**步骤**：
1. 返回 ConfigActivity
2. 取消所有复选框
3. 重新进入播放页面

**预期结果**：
- ✅ 视频正常播放
- ✅ 没有任何覆盖层显示
- ✅ 性能更好（无额外绘制）

---

### 测试 5: 部分启用控件

**测试组合 1**：只显示时钟
- [ ] 只勾选 "显示实时时钟"
- [ ] 验证只有时钟显示

**测试组合 2**：只显示计时器
- [ ] 只勾选 "显示直播计时器"
- [ ] 验证只有计时器显示

**测试组合 3**：只显示弹幕
- [ ] 只勾选 "显示弹幕文字"
- [ ] 验证只有弹幕显示

---

### 测试 6: 单视频场景

**步骤**：
1. 只选择一个视频
2. 进入播放页面
3. 尝试画 V 字手势

**预期结果**：
- ✅ 手势被识别但不会切换
- ✅ 日志输出："只有一个视频，无法切换"
- ✅ 当前视频继续播放

---

### 测试 7: 多视频循环

**前置条件**：准备 3 个视频

**步骤**：
1. 需要修改代码支持多视频列表（暂时手动添加）
2. 依次画 V 字手势切换

**预期结果**：
- ✅ 第 1 次 V 字：视频1 → 视频2
- ✅ 第 2 次 V 字：视频2 → 视频3
- ✅ 第 3 次 V 字：视频3 → 视频1（循环）

---

## 🔍 调试技巧

### 查看日志

```bash
# 过滤 PlayActivity 日志
adb logcat | grep PlayActivity

# 过滤 VGestureDetector 日志
adb logcat | grep VGestureDetector

# 查看所有相关日志
adb logcat | grep -E "PlayActivity|VGestureDetector|OverlayView"
```

### 关键日志输出

**成功的手势识别**：
```
D/PlayActivity: 检测到 V 字手势，切换视频
D/PlayActivity: 切换到下一个视频: 2
```

**失败的识别**：
```
// 无日志输出（静默失败）
```

**OverlayView 初始化**：
```
D/PlayActivity: OverlayView 已初始化
```

---

## ⚠️ 已知限制

### 当前版本的限制

1. **多视频列表 UI 未实现**
   - 当前只能通过 Intent 传递多个视频
   - ConfigActivity 只能选择单个视频
   - 测试多视频需要修改代码或使用 ADB

2. **Logo 水印未实现**
   - OverlayConfig 中有 showLogo 字段
   - 但 OverlayView 中未实现绘制逻辑

3. **控件位置固定**
   - 时钟固定在右下角
   - 计时器固定在左下角
   - 暂不支持自定义位置

4. **弹幕功能简化**
   - 只有一条弹幕
   - 文本固定为 "欢迎来到直播间"
   - 不支持多条弹幕同时显示

---

## 🐛 常见问题排查

### 问题 1: 看不到画面控件

**可能原因**：
- 未在 ConfigActivity 勾选对应的复选框
- OverlayConfig 未正确传递

**解决方法**：
1. 检查 ConfigActivity 是否勾选了控件
2. 查看日志确认 OverlayView 是否初始化
3. 检查 Intent 是否正确传递 EXTRA_OVERLAY_CONFIG

### 问题 2: V 字手势不生效

**可能原因**：
- 手势不符合要求（距离太短、时间太长）
- 只有一个视频
- onTouchEvent 未被调用

**解决方法**：
1. 确保画 V 字的位移超过 200px
2. 确保在 1 秒内完成
3. 确保至少添加了 2 个视频
4. 查看日志是否有错误

### 问题 3: 视频切换有黑屏

**可能原因**：
- switchToMediaSource 参数错误
- 播放器状态异常

**解决方法**：
1. 检查 ExoPlayerManager.switchToMediaSource 的实现
2. 确保第二个参数为 false（不重置位置）
3. 查看播放器日志

---

## 📊 性能监控

### 观察指标

1. **FPS（帧率）**
   - 正常应该保持 60 FPS
   - 开启控件后不应明显下降

2. **内存使用**
   ```bash
   adb shell dumpsys meminfo com.lingma.livebgplayer
   ```

3. **CPU 使用率**
   ```bash
   adb shell top | grep com.lingma.livebgplayer
   ```

### 预期性能

- 无控件：60 FPS，CPU < 10%
- 有时钟：60 FPS，CPU < 12%
- 有计时器：60 FPS，CPU < 12%
- 有弹幕：58-60 FPS，CPU < 15%
- 全部开启：55-58 FPS，CPU < 18%

---

## ✅ 测试完成检查表

完成以下所有测试后，标记为通过：

- [ ] ConfigActivity 新界面显示正常
- [ ] 三个控件复选框正常工作
- [ ] V 字手势提示卡片显示正常
- [ ] 时钟显示和更新正常
- [ ] 计时器显示和更新正常
- [ ] 弹幕滚动正常
- [ ] V 字手势识别准确
- [ ] 视频切换无缝
- [ ] 单视频场景处理正确
- [ ] 禁用控件后无残留
- [ ] 性能符合预期
- [ ] 无崩溃和 ANR

---

## 📝 测试报告模板

```
测试日期：2026-04-20
测试设备：[设备型号]
Android 版本：[版本号]
应用版本：v1.1-beta

测试结果：
✅ 通过：[列出通过的测试项]
❌ 失败：[列出失败的测试项]
⚠️ 警告：[列出需要注意的问题]

发现的问题：
1. [问题描述]
   - 重现步骤：...
   - 预期结果：...
   - 实际结果：...
   - 严重程度：高/中/低

建议改进：
1. [改进建议]
```

---

## 🎯 下一步

测试完成后：

1. **如果所有测试通过**：
   - 可以合并到 master 分支
   - 更新版本号为 v1.1
   - 创建 GitHub Release

2. **如果发现问题**：
   - 记录问题和重现步骤
   - 修复 Bug
   - 重新测试

3. **如果需要增强功能**：
   - 实现 Logo 水印
   - 添加多视频管理 UI
   - 优化弹幕效果
   - 添加更多控件类型

---

**祝测试顺利！** 🚀
